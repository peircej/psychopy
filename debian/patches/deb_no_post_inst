From: Yaroslav Halchenko <debian@onerussian.com>
Subject: Do not install Windows specific psychopy_post_inst.py
Origin: Michael Hanke <michael.hanke@gmail.com>

--- a/setup.py
+++ b/setup.py
@@ -47,7 +47,9 @@ elif platform=='posix':
 
 setup(name="PsychoPy",
     packages=packages,
-    scripts = ['psychopy_post_inst.py','psychopy/app/psychopyApp.py'],
+    # No need for win32 postinst script in Debian, nor treating psychopyApp
+    # as a script -- we invoke it via runpy
+    # scripts = ['psychopy_post_inst.py','psychopy/app/psychopyApp.py'],
     include_package_data =True,
     package_data = {
         # If any package contains *.txt or *.rst files, include them:
